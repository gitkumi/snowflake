# {{ .Name }}

Snowflake project.

## Setup

```sh
make app.tools.install
```

Note: Be sure to check the docs of each tools.

## Commands

**Project-wide:**
- `make audit` - Code quality checks
- `make tidy` - Format and tidy code

**App-specific:** See `cmd/app/Makefile` for development commands.
