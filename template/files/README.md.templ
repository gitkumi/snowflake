# Snowflake

## Getting Started

{{ if eq .Type "web"}}0. Make sure you have Node 20.x and install `pnpm`

```sh
npm i -g pnpm
```{{ end }}

1. Install these packages:

```sh
go install github.com/cosmtrek/air@latest &&
go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest &&
go install github.com/pressly/goose/v3/cmd/goose@latest &&
go install gotest.tools/gotestsum@latest &&
{{if eq .Type "web"}}go install github.com/a-h/templ/cmd/templ@latest &&
go install github.com/gzuidhof/tygo@latest &&
go install github.com/evanw/esbuild/cmd/esbuild@latest{{ end }}
```

2. Start the development environment:

```sh
make dev
````

## Stack

Snowflake is built with these packages. Make sure to check their documentation.

#### Dev

- [air](https://github.com/cosmtrek/air)
- [gotestsum](https://github.com/gotestyourself/gotestsum)

#### Routing

- [Gin](https://gin-gonic.com/)

#### Database

- [sqlc](https://github.com/sqlc-dev/sqlc)
- [goose](https://github.com/pressly/goose)

{{ if eq .Type "web"}}#### Front-end

- [templ](https://templ.guide/)
- [Tailwind](https://tailwindcss.com/)
- [esbuild](https://esbuild.github.io/)
- [tygo](https://github.com/gzuidhof/tygo){{ end }}

## Make Commands

- `make dev` - Start the development environment with hot reload.
- `make test` - Run tests.
- `make build` - Build the project.
- `make run` - Run the build.
- `make db` - Check database status.
- `make migrate` - Run database migration.
- `make create <table_name>` - Create an empty migration file.
